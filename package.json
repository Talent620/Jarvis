{
  "name": "jarvis",
  "version": "1.0.0",
  "private": true,
  "description": "JARVIS — futurystyczny asystent głosowy i tekstowy (powłoka Capacitor: Android / iOS).",
  "scripts": {
    "build": "node ./scripts/build-web.mjs",
    "sync": "cap sync",
    "sync:ios": "cap sync ios",
    "add:ios": "cap add ios",
    "license:genkeys": "node ./scripts/license.mjs genkeys",
    "license:issue": "node ./scripts/license.mjs issue",
    "license:verify": "node ./scripts/license.mjs verify"
  },
  "dependencies": {
    "@capacitor/app-launcher": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
